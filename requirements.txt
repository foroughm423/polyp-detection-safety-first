streamlit>=1.32.0
ultralytics==8.4.61
opencv-python==4.13.0
huggingface_hub>=0.20.0
python-dotenv>=1.0.0
imageio-ffmpeg>=0.4.9
numpy==2.2.6
Pillow==12.2.0

# For GPU support, install PyTorch separately before running:
# pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# Streamlit Cloud uses CPU-only inference (no GPU required for deployment).
torch==2.6.0